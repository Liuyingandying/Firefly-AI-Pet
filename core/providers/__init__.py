"""Provider catalog package (Phase 2.1).

Holds :mod:`core.providers.catalog` — the single source of truth for
provider base configuration (endpoint / model / credential env / status).
No routing order, no fallback policy, no provider lifecycle lives here.
"""
