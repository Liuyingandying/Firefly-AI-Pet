PySide6
websockets
keyboard==0.13.5
mem0ai==2.0.18
fastembed>=0.7,<1
PyYAML>=6,<7
pydantic>=2.7,<3
pydantic-settings>=2.2,<3

# ---------------------------------------------------------------------------
# PDF Multimodal Processing (Phase 10)
# ---------------------------------------------------------------------------
# Core PDF handling (already installed: PyMuPDF via pymupdf)
# Optional: install one OCR engine for scanned/image PDFs

# RapidOCR — fast, CPU-friendly, recommended default
# pip install rapidocr_onnxruntime
#
# EasyOCR — more accurate but slower, larger model
# pip install easyocr
#
# Choose one. Both are optional — the pipeline degrades gracefully
# (image PDFs without OCR return rendered images for vision model).
rapidocr_onnxruntime>=1.3,<2
